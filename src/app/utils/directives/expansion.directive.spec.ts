import { ExpansionDirective } from './expansion.directive';

describe('ExpansionDirective', () => {
  it('should create an instance', () => {
    const directive = new ExpansionDirective();
    expect(directive).toBeTruthy();
  });
});
